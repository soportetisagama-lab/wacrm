name: Build and Push Docker Image (Sagama Industrial)
on:
  push:
    branches: [xlr9-industrial]
jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ghcr.io/soportetisagama-lab/wacrm:xlr9-industrial
          build-args: |
            NEXT_PUBLIC_SUPABASE_URL=${{ secrets.INDUSTRIAL_NEXT_PUBLIC_SUPABASE_URL }}
            NEXT_PUBLIC_SUPABASE_ANON_KEY=${{ secrets.INDUSTRIAL_NEXT_PUBLIC_SUPABASE_ANON_KEY }}
            NEXT_PUBLIC_SITE_URL=${{ secrets.INDUSTRIAL_NEXT_PUBLIC_SITE_URL }}
